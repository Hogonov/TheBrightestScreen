# TheBrightestScreen

Бесплатное локальное приложение для повышения яркости Apple XDR на macOS 13+. Сборка для Apple Silicon, без зависимостей, подписки и сети.

## Запуск

Откройте `build/Brighter.app`. В строке меню появится солнце. Нажмите **Максимальная яркость**: Brighter выставит системную яркость на 100% и включит XDR. При обычном запуске усиление выключено. При желании скопируйте приложение в «Программы».

- **Включить XDR** повышает диапазон яркости, сохраняя текущую системную настройку.
- **Усиление XDR** регулирует дополнительное усиление от 0 до 100% (это не измерение яркости в нитах).
- **Выключить XDR** возвращает исходную гамму и системную яркость, если она всё ещё на выставленном приложением максимуме. Изменения клавишами яркости сохраняются.
- **Выключить и выйти** восстанавливает настройки и закрывает приложение.
- Сначала закройте BrightXDR, Vivid или BrightIntosh, если они запущены.

## Почему нет белого полноэкранного слоя

BrightXDR накладывает на весь экран яркий Metal-слой с multiply-фильтром; в его README указан белый экран при переключении Spaces. Brighter использует подход BrightIntosh: EDR-окно **1×1 точка** в углу экрана и гамма-таблицы CoreGraphics. У окна нет compositing filter, оно не принимает фокус и клики, не масштабируется до размеров экрана, остаётся stationary и доступно во всех Spaces. Metal-текстура имеет размер 1×1 пиксель. Это убирает полноэкранную поверхность, вызывающую описанный артефакт; не является гарантией отсутствия любых ошибок WindowServer на всех версиях macOS.

Гамма повышается плавно, после готовности Metal и HDR. При потере HDR сразу возвращается исходная гамма. При сне/смене пользовательской сессии эффект выключается и переинициализируется после возврата. При смене экрана поддерживается отдельное состояние каждого дисплея. Три последовательные проверки конфликта гаммы приостанавливают экран вместо бесконечного перезаписывания настроек другой программы.

## Реальный максимум и ограничения

Поддерживаются встроенные Apple XDR и внешние дисплеи с XDR в названии и доступным EDR; зеркалирование исключено. На обычном SDR-экране дополнительная яркость недоступна. Максимум зависит от профиля, температуры, питания, изображения и ограничений macOS. Приложение не измеряет ниты и не обещает постоянные 1600 нит по всей площади. Крошечная HDR-точка может быть заметна в углу. HDR-видео при усилении может терять детали в светах; для точной работы с цветом выключайте усиление.

Автоматическая установка системного ползунка использует необязательный private framework DisplayServices с проверкой символов во время запуска. Если API недоступен, XDR продолжает работать; выставьте обычную яркость клавишей F2. Автояркость и True Tone не отключаются принудительно и могут влиять на результат. Без них яркость обычно предсказуемее; Brighter не меняет эти настройки. Внешние XDR-дисплеи требуют отдельной проверки на оборудовании.

## Сборка и проверка

Нужны Xcode Command Line Tools и macOS:

```sh
bash scripts/build.sh
bash scripts/test.sh
build/Brighter.app/Contents/MacOS/Brighter --diagnose
build/Brighter.app/Contents/MacOS/Brighter --smoke-test
```

`--diagnose` только читает состояние. `--smoke-test` на 9 секунд включает максимум, проверяет реальное применение гаммы, ограниченный размер окон и восстановление, затем завершает процесс. Перед этим закройте уже запущенный Brighter. Этот тест вызывает обработчик смены Spaces, но сам рабочие столы не переключает.

Для проверки конкретной системы включите максимум, несколько раз переключите Spaces и полноэкранное приложение, откройте/закройте Mission Control, затем выключите XDR. Физическую яркость и отсутствие кратких вспышек следует оценивать на самом дисплее: скриншот не измеряет ниты.

Опция `--enable` запускает сразу с максимальной яркостью. Автозапуск не включается автоматически. `SIGTERM`, `SIGINT`, `SIGHUP` обрабатываются с восстановлением состояния; после принудительного `SIGKILL` восстановление системного ползунка не гарантируется.

Сборка подписана локальной ad-hoc подписью, без Apple notarization. Исходный код и лицензия приложены. Основано на подходе [BrightIntosh](https://github.com/niklasr22/BrightIntosh); см. `THIRD_PARTY_NOTICES.md` и `LICENSE`.
